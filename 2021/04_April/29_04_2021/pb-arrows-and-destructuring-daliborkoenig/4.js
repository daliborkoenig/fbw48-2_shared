/*

Assignment 4

Another place where you might use an anonymous function is as
a callback inside another function. For example, in the
Array.filter() or Array.forEach() methods.

1. Convert the anonymous function present inside the following callback to
use an arrow function

*/

const foobar = [ 'foo', 'bar' ];

foobar.forEach(function(item) {
    console.log(item);
});

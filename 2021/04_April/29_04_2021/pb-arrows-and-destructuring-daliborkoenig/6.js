/*

Assignment 6

We can use default values to ensure that values are not set to undefined, if the value
is not supplied into the function.

1. Update the following function, so that the argument "name" has a default value of "anonymous"

Further reading on default values:

(English)
https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Default_parameters

(Deutsch)
https://developer.mozilla.org/de/docs/Web/JavaScript/Reference/Functions/Default_parameters

*/

function foobar(name) {
    console.log(name);
}

foobar();
